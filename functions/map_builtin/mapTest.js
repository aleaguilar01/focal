const mapTest = (arr) => {
  return arr.map((x) => x.length);
};

const lighthouses = ["Gibraltar Point", "Peggy's Point", "Cove Island", "Discovery Island", "Cape Scott", "Point Clark", "Kincardine"];

console.log(mapTest(lighthouses));
